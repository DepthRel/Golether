<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Native components the application needs (libmpv for video, GStreamer for cameras and voice): a catalog of pinned
    downloads, verified installation into the user data folder without administrator rights, lookup of bundled copies
    and installation hints for systems where packages come from the distribution.
  -->
  <PropertyGroup>
    <RootNamespace>Golether.Components</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\Core\Golether.Core\Golether.Core.csproj" />
  </ItemGroup>

</Project>
