<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Build tool: downloads, verifies and prepares native components (libmpv, GStreamer) for packaging, so the
    published archives already contain them.
  -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <RootNamespace>Golether.Tools.Components</RootNamespace>
    <AssemblyName>golether-components</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Logging.Console" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\Components\Golether.Components\Golether.Components.csproj" />
  </ItemGroup>

</Project>
